# Core (the deterministic core runs with just these).
numpy>=1.24
pandas>=2.0
requests>=2.28
pyyaml>=6.0

# Optional — install what you need, or `pip install -e '.[all]'`.
# ML ranker / purged-CV experiments:
lightgbm>=4.0
scikit-learn>=1.3
scipy>=1.10
# Kalshi signing:
cryptography>=41.0
# Reliability diagrams:
matplotlib>=3.7
# Dev:
pytest>=7.4
ruff>=0.4
